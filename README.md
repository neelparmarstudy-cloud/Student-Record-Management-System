# Student-Record-Management-System
# Student Record Management System  A structural command-line workstation utility built in C designed to catalog, validate, track, and re-order student academic databases. The application uses memory pointers and explicit runtime dynamic memory allocations (`malloc` / `free`) to manage dataset bounds efficiently based on user parameters.
